// const calcGraphs = (nodes, from, to) => {
//   const resSum = [];
//   const map = new Map();
//   from.map((item, index) => {
//     if (from.indexOf(item) !== index) {
//       map;
//     }
//   });
// };

// const graphLength = (index, count, from, to) => {
//   if (index != -1) {
//     ++count;
//     newIdx = from.findIndex((item) => item === to[index]);
//     return graphLength(newIdx, count, from, to);
//   }
//   return count;
// };

// const deleteUsedCouple = (index, count, from, to) => {
//    if (index !== -1 )
// }
